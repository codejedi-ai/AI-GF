# Core — always required
python-dotenv>=1.1.0
livekit>=1.0.7
livekit-agents>=1.0.20
livekit-api>=1.0.2
livekit-plugins-openai>=1.0.20
livekit-plugins-silero>=1.0.11
livekit-plugins-noise-cancellation>=0.2.2
openai>=1.72.0
httpx>=0.28.1
aiohttp>=3.11.16
numpy>=2.2.4

# Optional providers — install only what your agent JSONs actually use.
# The provider registry imports these lazily, so a missing package only
# matters if a config names that provider.
#
# llm.provider = "google":
#   livekit-plugins-google
# llm.provider = "anthropic":
#   livekit-plugins-anthropic
# tts.provider = "elevenlabs":
#   livekit-plugins-elevenlabs
# tts.provider = "rime":
#   livekit-plugins-rime @ git+https://github.com/rimelabs/livekit-agents.git@bddcc0d265176bb6b3a6b32d6773e7980e08790c#subdirectory=livekit-plugins/livekit-plugins-rime
# llm/tts provider = "huggingface":
#   transformers
#   torch
# tts/stt provider = "silero" (local models via torch.hub):
#   torch
#   torchaudio
#   omegaconf
# tts.provider = "kokoro": no extra package (needs a running Kokoro-FastAPI server)
